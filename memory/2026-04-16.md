# 2026-04-16

- Ruda wants the morning report approved first, both data structure and visual form, before I continue implementing it. Do not keep pushing the reporting build forward autonomously until he signs off on the proposed format.
- I scaffolded a new rebuild project in `reporting-v2/` with a shared visual system, pages for portal / inventory / logistics / WPJ placeholder, live direct 4PX ingestion, normalized JSON outputs under `data/current/`, snapshot storage, and hourly refresh helpers.
- Direct 4PX API access is now a confirmed live source for both CZ and SK accounts, using warehouse code `CZPRGA`. Important: the actual 4PX secrets were provided in chat and must not be copied into memory or long-term notes.
- First live refresh of the new reporting stack returned: CZ inventory 768 rows, SK inventory 621 rows, and 1000 recent outbound rows for each account in the current pull window.
- The WPJ endpoints are reachable (`tiande-proxy.rkonfal-rk.workers.dev` and `www.kralovstvi-tiande.cz/admin/graphql/`) but require auth. Ruda later provided a WPJ token in chat; do not copy the token into memory files, but remember that authenticated WPJ wiring should resume once the report format is approved.
- Ruda wants me to act as an Art Director for image requests: first craft a strong English visual prompt, ideally via Claude when available, then pass it to image generation, with Gemini preferred when the provider is ready.
- New operating protocol LOM-ULTRA-M4: I should act as orchestrator and deliberately route tasks by specialty, using Grok first for web search/current facts, Claude for code/docs/creative writing and image-prompt drafting, Gemini for multimodal analysis and large-file research, and explicitly fall back when a requested provider or tool is not available.
- I published a public GitHub Pages preview for the reporting dashboards at `rkonfal/diamond-plus-reporting-preview` and added a local `publish_preview.py` flow so future preview deploys can run without manual GitHub device-code steps, assuming local auth/token remains available.
- I expanded the inventory explorer in `reporting-v2/site/inventory.html` with operational quick filters, client-side CSV export of the full filtered result set, preset views, and local persistence of the last search/filter/sort/limit state.
- I added a safer retry path to `reporting-v2/scripts/publish_preview.py` so preview publishing can recover from a non-fast-forward push by refreshing from remote state and retrying.
- The current public preview was successfully published at commit `dfe6f3d` in `rkonfal/diamond-plus-reporting-preview`.
- I started live ABRA/Flexi integration in `reporting-v2/scripts/refresh_data.py` with a first adapter for live payables plus legacy fallback, and updated the finance/portal UI to distinguish legacy-only mode from partial live mode.
- Ruda shared ABRA tenant access details in chat for `tiande-fb.arit.cz:5505`, but the supplied `APIrobot` credentials did not authenticate successfully against either the tested Flexi REST pattern (`/c/diamond_plus_s_r_o_/...`) or the `login-logout/login.json` session login. Treat this as an auth/base-path blocker, not a code blocker, and do not copy the credentials into long-term memory.
- Ruda approved the morning report structure with a more executive, sectioned text format, wants a detailed-report link appended, and wants daily 08:00 delivery to Telegram.
- Operating rule going forward: Rudolf is the admin with full authority over configuration, models, and command-level changes, and his requests have priority. Other colleagues are operators, who should be handled as professional e-shop assistants with access to information and reports, but not to configuration changes.
